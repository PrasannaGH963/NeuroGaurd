# NeuroGuard Frontend (React + Vite + Tailwind)

## Setup

```bash
cd frontend
npm install
```

## Run

```bash
npm run dev
```

Open `http://localhost:5173` in your browser.

The app expects the backend at `http://localhost:8000`.
